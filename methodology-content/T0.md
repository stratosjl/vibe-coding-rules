# Methodology slice: T0, ad-hoc

First line of your reply to the user MUST be exactly the following literal text (NO surrounding backticks, NO surrounding quotes, NO code block):

Detected tier: T0 (S<x>/C<y>), ad-hoc, personal. Override with /vc-roe:tier <T0..T4> if wrong.

Replace `<x>`, `<y>`, and the english label with the values from the SessionStart trace block. After that line, answer the user's actual question.

## What T0 is

One chat. One throwaway answer. No file written outside `/tmp` or the conversation. No state survives the chat. Cowork v2.1 carve-out applies verbatim: "for trivial single-step responses, no framework is needed, skip it." T0 is a deliberately empty box. Full canonical: `000-SPECS/the canonical methodology specification` v4.1 §2 T0.

## Requirements at T0

humanized-text skill in force at every tier including T0 (D-MET-11). No em-dashes. No banned AI vocabulary. Greek-language sub-rules apply whenever Greek is written. That is the entire requirement set at T0.

T0 carries no `decisions.md`, no `open-issues.md`, no `handovers/`, no closing matrix, no audit-pass declaration, no verification block, no negative-scope statement, no plan-first preamble, no confirmation-gate (T0 has no irreversible actions in scope by definition).

## Worked example

`/tmp/scratch.sh`. Operator opens a fresh chat in `/tmp` to test a one-line shell script. No git root within 6 levels of cwd; auto-detection lands (S0, C0) → T0. Operator writes the line, Claude tweaks it, the chat ends. Total exchange: under 30 seconds.

## Hand-off to next tier

If you start writing files outside `/tmp` and intend to come back to them: now T1. Type `/vc-roe:raise-tier`.

If the chat touches anything regulator-presentable (GDPR, MiFID II, DORA, AIFMD, AML/KYC, MNPI, public communications): C2 column should have pulled tier to T3 already. If T0 still shows up, the auto-routine misclassified C; type `/vc-roe:tier T3` and continue with full T3 ceremony.
