# Methodology slice: T4, mission-critical regulated (cumulative T0 + T1 + T2 + T3 + T4)

First line of EVERY reply from the time this slice is loaded (whether at SessionStart or mid-session via `/tier`, `/raise-tier`, or `/lower-tier`) until session end MUST be exactly the following literal text (NO surrounding backticks, NO surrounding quotes, NO code block). v1.13.0 clarifies: the rule applies to every reply, not only the first reply after slice load.

Detected tier: T4 (S<x>/C<y>), <label>. Override with /vc-roe:tier <T0..T4> if wrong.

## What T4 is

A project where a regulator (HCMC, ESMA, EBA, EIOPA, national DPA, internal audit body) can compel production of artefacts. A wrong calculation, missing audit trail, or missed deadline triggers a regulatory finding or personal-conduct issue. A project that ships infrastructure carrying real money or real personal data.

T4 is reached only via explicit operator decision: `tier: T4` sentinel in project CLAUDE.md (persistent), or live `/vc-roe:tier T4` (one-session). Auto-detection caps at T3 per D-MET-8. Tier inflation alienates the operator more than tier deflation misses regulatory evidence (the latter is recoverable next session, the former erodes trust). Full canonical: the canonical methodology specification v4.1 §2 T4.

## Cumulative requirements

**Inherited (T0 + T1):** humanized-text always-on; plan-first; verify-with-evidence; confirmation-gate for irreversible actions; capture-lessons inline; negative-scope at end.

**Inherited (T2 + v0.1.7 additions):** `decisions.md` + `open-issues.md` (stable IDs); per-session opening prompt rendered as fenced block in final chat message; pre-next-chat verification block; [EXAMPLE-PROJ] five-step session close; Brainstorming HARD-GATE convention; Session-health heartbeat every 30 min wall-clock (v0.1.7 hook-supported via SessionStart anchor + UserPromptSubmit clock-tag + Stop sentinel-grep, D-MET-39..D-MET-41; the clock tag is informational since v1.20.0, I-9); anomaly-callout inline at turn-level (T3+ adds formal OBS-row capture); TaskCreate for plans ≥3 steps; bounded project-brain snapshot (CLAUDE.md is a rewritten-each-close snapshot under the harness size threshold, with older session narrative in an indexed sidecar; T2 item 14).

**Inherited (T3):** Audit-pass policy (footer on every handover); lessons-promotion path; irreversible-action enumeration in CLAUDE.md; Four-pass closing matrix (Pass 1 edits | Pass 2 forward obligations | Pass 3 scope discipline | Pass 4 end-of-session sweep); CA3 hybrid handover split into summary + audit-trail (per D-MET-51 / v0.2.1; T4 widens audit-trail target to 10 to 30 KB / no hard ceiling but mandatory one-paragraph TL;DR at top); Anomaly-first reflex; Regulator-presentable exactness; Best-practices-first (performance, project management, security, scheduling); Long-process status checks (10-min cadence); Capture every idea; Three-same-day-override pattern; Deferral closure-date discipline; Source-of-truth chain (T3 item 12: forge-canonical, reconcile-before-work, close-time push, private-by-default; infrastructure-specific residency/DR routing supplied via the machine-local addon seam).

### Added at T4: the session-open cross-session audit, before any substantive work

Every completeness check below this line takes the register as given. Element 8a enumerates *the register's* open items against the handover; T3 Pass 4 carries an open-issues capture check; T3 item 11 surfaces any forward obligation or OBS row lacking a closure trigger. **None of them asks whether the register is complete.** Where a register file is missing, or a session ended without writing one, each of those checks passes over an empty or partial set and reports success. A check whose denominator is a file that does not exist cannot fail.

So at T4, before substantive work begins, audit scope, decisions and open issues **across all sessions**, not only against the last handover. Four steps, and the first is what makes it an audit rather than a re-read:

1. **Enumerate the sessions from an instrument the register does not control.** The commit history, the forge, the artefacts the work actually wrote, and never the handover directory. A record cannot be its own denominator: auditing the register with the register detects drift inside it and is structurally blind to what it omits. Compare what that instrument implies against the handovers and register entries on disk, then name every session that has work and no record.
2. **Prove the registers exist and were read.** A missing `decisions.md` or `open-issues.md` is an unmeasured result, not a clean one. Report the denominator: sessions found, sessions carrying a record, items in each register, and what could not be examined and why.
3. **Re-measure each open item rather than inheriting its status, and separate STATE from EVENT.** An item naming a state ("the tree is dirty", "the remote is ahead") is answered by a fresh measurement. An item naming an event ("ran stale tooling", "the job was skipped", "fell back to X") is not, and a clean present state is exactly what a successful recovery looks like, so ask what the event caused rather than whether it persists.
4. **Surface the result to the operator before continuing**, gaps named, and set a closure trigger on anything carried forward that lacks one. T3 item 11 discharges here rather than separately.

**This is a completeness check, not a re-litigation.** An item confirmed still open is carried forward, not reopened for debate; a decision found unrecorded is written down as it was made, not re-decided. The audit reports, the operator disposes.

**Why it sits at T4 and not lower.** It costs a few minutes at every session open, and it repays only where the record is itself a deliverable, where a regulator can compel it and where the next session acts on the register without checking it. Below T4 the close-side checks are proportionate to the risk.

**Measured 2026-09-03**, on a project that had run twelve sessions at T3. Neither register file existed at all, so both close-side checks had been passing over an empty set for the life of the project. The commit history showed one session with two substantive commits and no handover, made on a second machine under a different commit identity, so neither its transcript nor its record was present on the machine doing the audit; three further work episodes carried commits and no numbered handover. Every one of them was invisible to a check that starts from the handover directory, and all of them surfaced from the commit log in a single command.

### Added at T4: the eleven-element structural close (abstract shape per D-MET-12)

The canonical T4 specification describes the SHAPE of a T4 close. It does NOT mandate specific artefact paths or technologies. Each T4 project chooses its own analogues.

1. **Regression floor verified** at session close. Project defines its regression floor (test suite, smoke test, latency check, compliance probe, backtest). Session close says: ran it, here is the result, here is the exit code or pass count.
2. **Change log entry** appended to a project-canonical change log (`BUILD_LOG.md`, `CHANGELOG.md`, project-specific).
3. **Operational runbook update** if operating procedures changed; if unchanged, state explicitly in negative scope.
4. **Compliance trace update** if compliance-relevant behaviour changed. Regulator-presentable exactness rule applies.
5. **Public-contract verification** if project exposes an API, file format, or other public contract. Lint clean.
6. **Architecture decisions captured** in change log (always) and architecture document (if architecture-grade). Free-form ADR format.
7. **Architecture-section row updated** if scope evolved within multi-phase plan (S-cycle, E-cycle, sub-cycle).
8. **Next-handover skeleton filled in**, with next-chat opening prompt rendered as fenced block in final chat message of closing session. Chat-render is a separate obligation from writing it into the handover; never substitute one for the other.

    **8a. The handover is re-audited against everything known at the moment of close, and the re-audit is the LAST thing done.** Element 11 already says the change log is written last; this says the handover is *audited* last. Any result arriving after the handover was drafted, a returning background job, a late review, a test that finished, makes the handover stale in the one artefact the next session acts on without checking. Enumerate the register's open items and confirm each is referenced or deliberately excluded, and that **no instruction in the handover is contradicted by anything now known**. The recorded failure is a handover that instructed the next session to run a process which later findings had blocked: the register was correct and the handover was confidently wrong. If anything lands after the audit, run it again.

    **8b. The prompt must live in an artefact, and the pointer must resolve.** The fenced chat block satisfies the render obligation and nothing else, because scrollback is not in the repo, does not survive a new machine, and cannot be read by whoever picks the work up. The handover carries the prompt inline or names a path that is **verified to exist before close**. At T4 the prompt also states what is BLOCKED and by which open item, not merely what to do next: an ordered list of actions with no blockers named is how a next session executes a forbidden step in good faith.
9. **User-facing or stakeholder-facing docs reviewed** for any drift from new behaviour.
10. **Final smoke check** passed before change log is closed. Project-defined live verification (brain-restart + `/health` probe; dev-server-renders-home-page; contract-validator-emits-clean; whatever fits).
11. **Change log written last**, after all other artefacts are stable.

### Plus three sub-procedures

- **Mid-flight pause sub-procedure.** Significant scope shift mid-session: stop, document the shift, re-confirm with operator before continuing. The pause is a confirmation gate, not a courtesy.
- **Multi-module review cadence** for project's own methodology document. Default: every five sub-cycles.
- **Pre-drafted handover skeletons** for next N sessions if project is on a known phase trajectory.

## Reference instance shape

The plugin does NOT ship a reference-instance file; that document lives in each operator's own working directory. The shape an operator-side reference instance typically takes:

| Abstract element | Typical instantiation |
|---|---|
| Regression floor | language-native test runner full pass + targeted regression of changed area |
| Change log entry | `BUILD_LOG.md` or `CHANGELOG.md` row appended after smoke check passes |
| Operational runbook | `RUNBOOK.md` if ops changed; otherwise stated in negative scope |
| Compliance trace | `COMPLIANCE.md` traceability-matrix update for any new auditable event type, one row per event |
| Public-contract | `openapi.yaml` (or equivalent contract spec) + lint clean |
| Architecture decisions | change-log row always; architecture-doc / ADR row if architecture-grade |
| Architecture-section | multi-phase-plan row in the spec for any sub-cycle that evolved scope, and for any enforcement-grade change |
| Next-handover skeleton | `handovers/session-(NN+1)-opening-prompt.md` |
| User-facing docs | reviewed for drift from new behaviour |
| Final smoke check | service restart + health probe + targeted route exercise |
| Change log written last | yes |

Each T4 project picks paths that fit its own stack. The methodology mandates the SHAPE of the close, not the specific filenames or technologies.

## What T4 deliberately does NOT require

T4 does NOT mandate any specific reference-instance artefacts; each T4 project keeps its own change-log path, runbook path, compliance-trace path, contract location, ADR format. T4 does NOT require slash-command override every session (once `tier: T4` is in CLAUDE.md, every session in that project starts at T4). T4 does NOT mandate any specific tooling, language, runtime, or platform.

## Worked example

A regulated-API project close instantiates elements 1 to 11 exactly as the reference table above shows. Element 3 had nothing to record, so the runbook was declared unchanged in negative scope. Element 8 was both written into the handover and rendered as a fenced block in the final chat message, two obligations rather than one. Element 11 was written last, after every other artefact was stable. Mid-flight anomalies were logged inline as they appeared, anomaly-first each time, with pause-and-probe rather than log-and-continue.

## Hand-off, there is no T5

T4 is the top of the matrix. If a project somehow grows beyond T4 (multi-organisational, multi-jurisdictional, with audit obligations across regimes), split into multiple T4 projects coordinated by a higher-level governance structure. The methodology does NOT prescribe what that governance looks like; out of scope.
